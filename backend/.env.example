# Municipality Address Check - ArcGIS live fetch config
#
# Copy this file to `backend/.env` and edit as needed.
# Environment variables use the prefix MAC_ (see backend/app/config.py).

# Optional: enable geocoding via Nominatim (OSM). For fully-offline, set false.
MAC_ALLOW_NOMINATIM=true

# Optional: protect admin refresh endpoint (POST /api/admin/refresh-datasets)
# MAC_ADMIN_TOKEN=change-me-to-a-strong-token

# --- ArcGIS layer endpoints (official sources) ---
# These are the ArcGIS REST *layer* URLs (must end in /<layerId>).
#
# Municipality boundary (eThekwini)
MAC_ETHEKWINI_MUNICIPAL_LAYER_URL=https://gis.durban.gov.za/server/rest/services/Hosted/Police_bounds_ethekwini/FeatureServer/0

# NSC (North/South/Central) – commonly derived from eThekwini zoning/scheme region datasets.
# Note: this layer can be large; the fetcher caches it to backend/data/nsc_regions.
MAC_ETHEKWINI_NSC_LAYER_URL=https://gis.durban.gov.za/server/rest/services/WebViewers/External_Cadastral_Services_Publisher/MapServer/28

# MPR (Municipal Planning Regions) – planning regions polygon layer.
MAC_ETHEKWINI_MPR_LAYER_URL=https://gis.durban.gov.za/server/rest/services/WebViewers/Infrastructural_Services_Publisher/MapServer/59

# Optional: set a safe User-Agent for ArcGIS/Nominatim requests
# MAC_NOMINATIM_USER_AGENT=municipality-address-check/1.0 (contact: you@example.com)
